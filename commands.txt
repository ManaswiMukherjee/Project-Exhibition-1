sudo firewall-cmd --add-port=8000/tcp       -- to open port
adb reverse tcp:8000 tcp:8000               -- to enable debugging for the phone to access camera
uvicorn main:app --host 0.0.0.0 --port 8000 -- 
ngrok http 8000                             -- to enable tunneling through https